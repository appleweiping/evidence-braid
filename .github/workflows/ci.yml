name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python-version: "3.11"
          - os: ubuntu-latest
            python-version: "3.12"
          - os: ubuntu-latest
            python-version: "3.13"
          - os: ubuntu-latest
            python-version: "3.14"
          - os: windows-latest
            python-version: "3.12"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: "0.11.21"
          enable-cache: false
      - run: uv sync --frozen --extra dev
      - run: uv run --frozen --extra dev python -m ruff check .
      - run: uv run --frozen --extra dev python -m ruff format --check .
      - run: uv run --frozen --extra dev python -m mypy
      - run: uv run --frozen --extra dev python -m bandit -q -c pyproject.toml -r src
      - run: uv run --frozen --extra dev python -m pytest --cov=evidence_braid --cov-report=term-missing
      - name: Smoke-test fixed-time evaluation
        # `$DECISION_OUTPUT` is bash syntax. Without an explicit shell the Windows
        # runner uses PowerShell, where it expands to nothing and the command runs
        # with an empty --output.
        shell: bash
        env:
          DECISION_OUTPUT: ${{ runner.temp }}/decision.json
        run: uv run --frozen --extra dev evidence-braid evaluate examples/policy.json examples/events.jsonl --as-of 2026-08-31T12:00:00Z --output "$DECISION_OUTPUT"
      - name: Smoke-test synthetic evaluation protocol
        if: runner.os == 'Linux' && matrix.python-version == '3.14'
        env:
          SYNTHETIC_OUTPUT: ${{ runner.temp }}/synthetic-evaluation.json
        run: uv run --frozen --extra dev python experiments/synthetic_baselines.py --samples 12 --repeats 3 --replay-events 8 --output "$SYNTHETIC_OUTPUT"
      - name: Build distributions
        run: uv run --frozen --extra dev python -m build --no-isolation
      - name: Smoke-test the isolated wheel
        if: runner.os == 'Linux'
        run: |
          python -m venv /tmp/evidence-braid-wheel
          /tmp/evidence-braid-wheel/bin/python -m pip install --no-index dist/*.whl
          cd /tmp
          /tmp/evidence-braid-wheel/bin/evidence-braid evaluate "$GITHUB_WORKSPACE/examples/policy.json" "$GITHUB_WORKSPACE/examples/events.jsonl" --as-of 2026-08-31T12:00:00Z --output decision-from-wheel.json
          /tmp/evidence-braid-wheel/bin/python -c "import json; assert json.load(open('decision-from-wheel.json', encoding='utf-8'))['policy_id'] == 'operations-demo-v1'"

  gate:
    name: CI gate
    if: ${{ always() }}
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Require every matrix job to succeed
        env:
          MATRIX_RESULT: ${{ needs.test.result }}
        run: test "${MATRIX_RESULT}" = success
